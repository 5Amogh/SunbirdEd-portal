import { FilterChartPipe } from './filter-chart.pipe';

describe('FilterChartPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChartPipe();
    expect(pipe).toBeTruthy();
  });
});
