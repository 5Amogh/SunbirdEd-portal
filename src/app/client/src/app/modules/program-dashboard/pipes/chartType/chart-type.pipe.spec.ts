import { ChartTypePipe } from './chart-type.pipe';

describe('ChartTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ChartTypePipe();
    expect(pipe).toBeTruthy();
  });
});
